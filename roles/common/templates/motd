--------------------------------------------------------
	  Servidor web

Hostname.: "{{ ansible_fqdn }}"
Enderco IP.: "{{ ansible_all_ipv4_addresses }}"

--------------------------------------------------------
